# mlboard

This project aims to create a leaderboard for ml competitions. Admins can create machine learning competitions and specify the input, the output, a time limit for execution and a test dataset. Users can upload models and get ranked. later the option to create and be a part of teams my be added.

```
docker-compose up
```